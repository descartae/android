mutation AddToWaitlist($email: String!, $latitude: Float!, $longitude: Float!) {
    addWaitingUser(input:{
               email: $email
               coordinates: {
               latitude: $latitude
               longitude: $longitude
               }
           }) {
        success
    }
}