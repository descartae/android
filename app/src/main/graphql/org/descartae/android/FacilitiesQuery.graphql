query FacilitiesQuery($latitude: Float!, $longitude: Float!) {
  facilities(filters: {cursor: {quantity: 100}, location: {near: {latitude: $latitude, longitude: $longitude}}}) {
    items {
      _id
      name
      location {
        address
        municipality
        state
        zip
        coordinates {
            latitude
            longitude
        }
      }
      typesOfWaste {
        icons {
          androidSmallURL
        }
      }
    }
  }
}